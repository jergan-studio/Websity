name: Build and Release Websity Windows App

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout Websity
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Compile Java
        shell: cmd
        run: |
          if exist build rmdir /s /q build
          mkdir build
          javac -d build src\main\java\App.java

      - name: Create JAR
        shell: cmd
        run: |
          if exist dist rmdir /s /q dist
          mkdir dist
          mkdir dist\app
          jar --create --file dist\Websity.jar --main-class App -C build .
          xcopy /E /I /Y web dist\app\web
          copy /Y dist\Websity.jar dist\app\Websity.jar

      - name: Create Windows EXE
        shell: cmd
        run: |
          jpackage ^
            --type exe ^
            --name Websity ^
            --input dist\app ^
            --main-jar Websity.jar ^
            --main-class App ^
            --dest dist ^
            --app-version 1.0.0 ^
            --win-dir-chooser ^
            --win-menu ^
            --win-shortcut

      - name: Create download copy
        shell: cmd
        run: copy /Y dist\Websity-1.0.0.exe dist\Websity.exe

      - name: Upload Websity JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: Websity-JAR
          path: dist/Websity.jar
          if-no-files-found: error

      - name: Upload Websity Windows EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: Websity-Windows-EXE
          path: dist/Websity.exe
          if-no-files-found: error

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Websity Latest
          prerelease: false
          make_latest: true
          overwrite_files: true
          files: |
            dist/Websity.exe
            dist/Websity.jar
